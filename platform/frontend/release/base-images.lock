# Resolve and review these digests before a production build. The release
# script refuses a mutable tag when a digest is supplied through the lock.
NODE_IMAGE=node:22-alpine
NGINX_IMAGE=nginx:1.27-alpine
