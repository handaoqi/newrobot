# Shared by roamerx-edge-agent.service and roamerx-bike-bot.service.
# Both run as `robot`; the detection process atomically replaces this file and
# the Edge Agent only reads it.  tmpfiles creates it at boot without granting
# either service permission to create arbitrary directories under /run.
d /run/roamerx 0770 robot robot -
