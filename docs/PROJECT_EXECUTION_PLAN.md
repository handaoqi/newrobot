# 项目执行计划

更新时间：2026-08-21

本文件是下次开发会话的恢复入口。执行时按顺序推进，完成一项后更新状态、提交测试结果和相关提交 SHA。

## 待执行任务

1. **冻结工作区**（已完成：2026-08-21）
   区分本轮代码改动、运行时数据和无关产物，排除 `.viminfo` 等文件。

2. **分析 `deploy/`**（已完成：2026-08-21）
   盘点云端、NX、3588 的部署入口，明确与 `runtime/`、systemd、Docker 的边界、前置条件和回滚方式。

3. **统一部署目录**（已实现并完成本地验证：2026-08-21）
   已新增 `deploy/platform/`、`deploy/nx-edge/`、`deploy/3588/` 正式入口；`runtime/` 中旧部署脚本改为兼容转发入口。bash 语法检查和三端 dry-run 已通过；真实端点部署留待发布阶段。

4. **三端数据备份**（已实现并完成 dry-run 验证：2026-08-21）
   已新增 `deploy/backup/backup-all.sh`、校验脚本和临时目录恢复验证脚本。标准备份包含配置、数据库、地图和状态；日志、rosbag/MCAP、缓存、模型、媒体和构建产物只进入排除范围，不复制到标准备份。真实备份留待确认 SSH 和现场状态后执行。

5. **建图闭环**（待执行）
   验证建图启动、IMU/首关键帧确认、保存、停止和云端上传的完整流程。

6. **定位恢复与任务地图**（待执行）
   验证定位丢失自动恢复，以及三个任务页面的丢失位置、机器人当前位置和任务控制字段。

7. **后端测试**（已完成：2026-08-21）
   已按当前“首个唤醒音节可替换、核心音节必须匹配”的设计修正模糊唤醒词测试断言；`monitoring` 全量 90 项通过。

8. **Docker 平台验收**（进行中：新库/升级库验收已完成，Compose 待安装 Docker 后执行）
   已验证迁移与幂等初始化：12 个播报模板、5 个技能绑定、1 个机器人，地图/路线/任务/排班为空；`monitoring.test_initialization` 3 项通过。完整 `monitoring` 测试 140 项通过，另有 1 项因虚拟环境缺少 `pytest` 导入失败；Compose 和真实备份恢复留待 Docker/目标端点可用后执行。

9. **清理旧地图 API**（待执行）
   清理或明确废弃 `map_api_server` 入口及其引用。

10. **提交、推送和部署**（待执行）
    完成代码审查、测试、提交并推送 `main`，从确定的提交 SHA 部署，最后做三端健康检查。

## 约束与验收

- 不覆盖或回退用户已有改动；先记录工作区状态，再做移动和重构。
- 备份不能包含临时构建目录、缓存和重复的大型产物；凭据需单独保护并在清单中标记。
- 部署前必须能执行 dry-run 或等价检查，部署后必须有服务状态、健康接口和回滚路径。
- 云端 systemd 与 Docker 平台不得同时使用同一数据库和 MQTT 身份。
- 每项完成后更新本文件状态，并记录测试命令与结果。

## 本轮执行记录

- 工作区冻结基线：当前 `main` 工作区存在用户未提交改动，执行过程中不得回退或覆盖。
- 新增部署入口：`deploy/platform/`、`deploy/nx-edge/`、`deploy/3588/`。
- 新增备份入口：`deploy/backup/backup-all.sh`、`verify-backup.sh`、`restore-check.sh`。
- Edge Agent 全量测试：110 项通过。
- 后端 `monitoring` 全量测试：90 项通过。
- 尚未执行真实三端备份、远端部署或任何会启动运动/充电的操作。
