# 任务 6：MPPI/RPP `wz` 振荡专项计划

- 计划建立日期：2026-09-12
- 状态：待后续独立推进
- 与当前任务关系：不属于任务 7–10，本次不实施、不调参、不部署

## 目标

观察 MPPI 与 RPP 局部控制器的参数和控制循环，定位角速度 `wz` 振荡来源，在不降低避障安全性和到点精度的前提下减少摆动。

## 后续步骤

1. 分别录制 MPPI、RPP 的参考路径、局部代价地图、定位姿态、里程计和输入/输出速度。
2. 按直线、弯道、绕障、到点转向四种工况量化 `wz` 峰值、过零次数、频率和路径横向误差。
3. 区分参考路径锯齿、定位噪声、控制频率/延迟、控制器参数四类原因。
4. MPPI 与 RPP 分开建立最小参数改动集和回滚基线，单变量对比验证。
5. 仿真/回放通过后，再安排低速实机 A/B 验收与独立提交、部署。

## 边界

- 不与 Smac Hybrid A*、iLQR、路径平滑、恢复行为或 Collision Monitor 的实现提交混合。
- 不以放宽碰撞阈值、关闭安全层或降低定位健康门限换取视觉上的平稳。
- 未完成数据对比前，不修改现网 MPPI/RPP `wz` 参数。
