# 3588 Installation Assets

`charge-pile/` is the validated ARM64 charging-station vendor bundle.
`vendor-archives/` preserves the original supplied archives. The core motion
eggs and `robot-launch` remain part of the vendor system image and are recorded,
not copied or replaced by this repository.

