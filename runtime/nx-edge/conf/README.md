# NX Configuration

Real Edge, RTK and vision configuration files are stored here with mode `0600`.
Only examples and this README are tracked. Legacy `/home/robot` paths are
compatibility links created by `scripts/migrate-runtime-data.sh`.

